#### 自动化部署安装脚本

1.配置免密登录

```bash
#!/bin/bash
#
IP_ADRESS=(192.168.124.{1..100})
if [ !-f ~/.ssh/id_rsa ]; then
	ssh-keygen -t rsa -b 2048 -N "" -f /.ssh/id_rsa
fi
for i in {$IP_ADRESS[@]}
do
(
	/user/bin/expect <<EOF 
	Spawn ssh-copy-id -o StrictHostKeyChecking=no root@$i
	expect {
		"yes/no" { send "yes\r" ; exp_continue }
		"password" { send "123456\r" }
	}
	expect eof
EOF
) &
done
wait
echo "所有主机公钥推送完毕"
```

注意：如首次安装可能需要指纹验证，需要expect自动验证安装expect

安装mysql

```bash
#1/bin/bash
#安装mysql仓库
dnf inatall -y    *** & >/dev/null
#检查是否已安装启用，未安装开始安装
dnf repolist enabled | grep 'mysql'
if [ $? -eq 0 ] && dnf install -y mysql-community-server --nogpgcheck
#查看是否安装成功
rpm -q mysql-community-server
if [ $? -eq 0 ]; then
	systemctl start mysql
	systemctl enable mysql
else 
	echo "安装失败"
	exit
fi
#获取密码
grep passwd /var/log/mysqld.log | awk '{print $NF}' >/root/mysql_pass.txt
TEMP_PASSWORD= $(cat "/root/mysql_pass.txt")
#设置密码
#登录Mysql
MYSQL_ROOT_PASSWORD="Aa123456@"
mysql --connect-timeout=10 -uroot -p"$TEMP_PASSWORD" --connect-expired-password <<EOF
#设置新的密码 %或localhost 其他主机或者所有主机都能连
ALTER USER 'root'@'localhost' IDENTIFIED BY 'MYSQL_ROOT_PASSWORD';
CREATE USER 'root'@'%' IDENTIFIED BY 'MYSQL_ROOT_PASSWORD';
#删除匿名用户
DELETE FROM mysql.user WHERE User='';
#删除test数据库
DROP DATABASE IF EXISTS test;
#刷新权限立即生效
FLUSH PRIVILEGES;
EOF

#把/usr/local/zzyl_project/sql_data 导入mysql
cd /usr/local/zzyl_project/sql_data
mysql -uroot < ry-zzyl_20240913.sql -pAa123456@



  

```

![image-20260909081415865](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260909081415865.png)

mysql卸载重装

```
rpm -q | grep mysql
rpm -e mysql80-community-release-el9.x86_64  --nodeps
相关软件卸载
yum remove mysql-server
rm -rf /etc/my.conf
rm -rf /var/log/mysqld.log
```

软件有日志看日志，没有日志看系统日志



安装 redis

```
#!/bib/bash
#一键安装配置redis
#安装
dnf install -y redis
#配置 sed -i 
sed -i '75s#127\.0\.0\.1#0\.0\.0\.0#g' /etc/redis/redis.conf
sed -i '900a requirepass 123456' /etc/redis/redis.conf
#启动

```

配置JDK

```
mkdir -p /export/sotware
cd //export/sotware
dnf install wget -y
wget https.....   
tar -xf openjdk-11.0.0.2_linux-x64.tar.gz -c /usr/local
#添加环境变量
echo "export JAVA_HOME=/user/local/openjdk-11.0.0.2" >> /etc/profile
echo "export PATH=$JAVA_HOME:/bin:$PATH >> /etc/profile
echo "export CLASSPATH=$JAVA_HOME/lib:$CLASSPATH >> /etc/profile
soure /etc/profile



```

tomcat

![image-20260909084836758](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260909084836758.png)

tail -100 logs/catalina.out

![image-20260909085607114](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260909085607114.png)

mysql -e "grant all on *.* to root@'%';" -pAa123456@

./bin/shutdown.sh

./bin/startup.sh



前端nginx

```
dnf install nginx
mkdir -p /var/www/
#防火墙开启http、https、80、443
#npm run build:prod   打包前端在dist/中
mv /root/dist.zip /var/www
cd /var/www/
unzip dist.zip
rm -rf dist.zip


vim /etc/nginx/nginx.conf
#添加三行CORS 跨域资源共享
#在server 配置客户端请求大小限制
#处理静态页面
location / {
	root /var/www/dist; 
	index 
}
#处理反向代理
location /prod-api {
	proxy_pass 转发url
	proxy_set_header upgrade $http_upgrade;
	proxy_set_header connection upgrade;
}
 
 





```



  